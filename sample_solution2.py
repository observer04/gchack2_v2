# All imports here
import argparse
import os


def maskgeration(imagepath, model_path):
    # Load Your Model Here
    # model = YourModel()
    # model.load_state_dict(torch.load(model_pth, map_location="cpu"))
    # model.eval()

    # Your Code Here
    print("Your Code")
    # Save the mask corresponding to each input with the SAME filename as reference band
